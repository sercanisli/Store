@model IEnumerable<Category>

<div class="text-center">
    <h6 class="display-">Categories</h6>
</div>

<ul class="list-group my-3">
    @foreach (var category in Model)
    {
        <li class="list-group-item">@category.CategoryName</li>
    }
</ul>